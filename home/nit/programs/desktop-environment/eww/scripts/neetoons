#!/bin/sh

status=$(curl -s --max-time 5 "http://localhost:3007/status" | jq -r ".status")
exit_code=$?

if [ $exit_code -eq 0 ] && [ "$status" == "online" ]; then
    echo "󱙺"
else
    echo "❌"
fi
